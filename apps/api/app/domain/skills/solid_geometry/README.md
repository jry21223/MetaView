# Solid Geometry Skill Pack Stub

This directory reserves the future solid-geometry SkillPack boundary.

V1 scope:

- Detect prompts that likely need a solid-geometry problem spec.
- Keep the future output contract aligned with `PlaybookScript` plus `DirectorScript`.
- Emit future `DirectorBeat.focus_target` values such as `segment:Line_BE`.

Out of scope for this stub:

- Geometry theorem solving.
- Sympy-backed construction kernels.
- 3D camera execution.
- Renderer changes.
